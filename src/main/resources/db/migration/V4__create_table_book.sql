DROP TABLE IF EXISTS book CASCADE;
CREATE TABLE IF NOT EXISTS book(book_id SERIAL PRIMARY KEY,title VARCHAR NOT NULL UNIQUE,author_id INT NOT NULL REFERENCES author(author_id) ON DELETE CASCADE,category_id INT NOT NULL REFERENCES category(category_id) ON DELETE CASCADE,published_year VARCHAR NOT NULL,language_id INT NOT NULL REFERENCES language(language_id) ON DELETE CASCADE);